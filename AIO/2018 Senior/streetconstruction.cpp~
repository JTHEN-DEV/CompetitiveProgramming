#include <bits/stdc++.h>

using namespace std;

int main(){
    //freopen("streetin.txt", "r", stdin);
    //freopen("streetout.txt", "w", stdout);
    int n, p;
    cin >> n >> p;
    cout << (n/(p+1));
}
