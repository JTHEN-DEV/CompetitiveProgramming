#include <bits/stdc++.h>

using namespace std;

int main(){
    int n;
    cin >> n;
    int min = 0;
    int total  = 0;
    while (n--){
        int a;
        cin >> a;
        total += a;
        if (total < min){
            min = total;
        }
    }
    cout << -1 * min << endl
}
