#include <bits/stdc++.h>

using namespace std;

int main(){
    int k, r;
    cin >> k >> r;
    int n = k;
    int a = 1;
    while (!(n%10 == 0 || n%10 == r)){
        n += k;
        a++;
    }
    cout << a << endl;
}
