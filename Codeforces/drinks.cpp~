#include <bits/stdc++.h>

using namespace std;

int main(){
    int n, a, s;
    long double avg;
    s = 0;
    cin >> n;
    for (int i = 0; i < n; i++){
        cin >> a;
        s += a;
    }
    avg = (double)s/n;
    cout << avg;
}
