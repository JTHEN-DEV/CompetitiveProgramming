#include <bits/stdc++.h>

using namespace std;

int main(){
    string a, b;
    cin >> a >> b;
    string s = "";
    for (int i = 0; i < a.length(); i++){
        s += to_string((a[i]-'0')^(b[i]-'0'));
    }
    cout << s;
}
