#include <bits/stdc++.h>

using namespace std;

int main(){
    int a, b, c;
    cin >> a >> b >> c;
    int n = 101;
    int mi = 500;
    while (--n){
        mi = min(mi, abs(n - a) + abs(n - b) + abs(n - c));
    }
    cout << mi << endl;
}
